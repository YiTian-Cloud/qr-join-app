"use client";

// Side-effect import so Firebase client init runs once in the browser
import "@/lib/firebase.client";

export default function ClientInit() {
  return null; // nothing to render
}
